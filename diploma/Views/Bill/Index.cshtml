@model IEnumerable<diploma.Models.Core.Bill>


<table class="table">
    <tr>
        <th>
            Абонент
        </th>
        <th>
            Номер счёта
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Client.Phone)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Number)
        </td>
        <td>
            @Html.ActionLink("Просмотр", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Удалить", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
